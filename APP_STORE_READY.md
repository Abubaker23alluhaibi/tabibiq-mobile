# جاهزية TabibiQ للرفع على App Store

## آخر مراجعة: فبراير 2025

---

## ✅ الناحية التقنية في المشروع (جاهز)

| البند | الحالة | ملاحظة |
|-------|--------|--------|
| **Bundle ID** | ✅ | `com.tabibiq.platform` (ثابت ولا يغيّر بعد أول رفع) |
| **النسخة** | ✅ | `version: "1.0.2"` و `ios.buildNumber: "1.0.2"` |
| **أذونات iOS مع النص** | ✅ | الموقع، الإشعارات، الكاميرا، معرض الصور – كلها مع Usage Description |
| **الموقع** | ✅ | للاستخدام أثناء التطبيق فقط (لا خلفية) |
| **Firebase iOS** | ✅ | `GoogleService-Info.plist` موجود في جذر المشروع |
| **EAS Build** | ✅ | ملف `eas.json` فيه profile اسمه `production` لـ iOS |
| **الأيقونة والسبلاش** | ✅ | `assets/icon.png` و `splash-icon.png` و adaptive-icon |
| **سياسة الخصوصية** | ✅ | ملف `PRIVACY_POLICY.md` – تحتاج **رابط عام** له في App Store Connect |
| **حذف الحساب فعلياً** | ✅ | زر "حذف حسابي" يحذف من الخادم وقاعدة البيانات (مواعيد، تقييمات، إشعارات، المستخدم) – متوافق مع متطلبات Google/Apple |

---

## 📋 ما تحتاجه أنت (خارج الكود)

### 1. حساب Apple Developer
- اشتراك **Apple Developer Program** (حوالي 99 دولار/سنة).
- الرابط: [developer.apple.com/programs](https://developer.apple.com/programs/).
- بدون هذا الحساب لا يمكن رفع التطبيق أو استخدام TestFlight.

### 2. في App Store Connect (بعد إنشاء التطبيق)
- **اسم التطبيق** و**الوصف** و**الكلمات المفتاحية**.
- **رابط سياسة الخصوصية**: يجب أن يكون رابطًا يعمل (مثلاً صفحة على موقعك أو GitHub Pages) – لا يكفي وجود الملف محليًا فقط.
- **لقطات شاشة** للأيفون (بالأحجام المطلوبة).
- **فئة التطبيق** (مثلاً Medical أو Health & Fitness).
- **تصنيف العمر** و**معلومات التلامس والدعم**.

---

## 🔨 بناء ورفع iOS

### البناء (على سحابة EAS)
```bash
cd TabibiQ-Mobile3
eas build --platform ios --profile production
```
- أول مرة سيُطلب منك تسجيل الدخول بـ **Expo** وربط **Apple ID** (حساب المطور).
- EAS ينشئ الشهادات والـ Provisioning إذا اخترت "remote" credentials.
- بعد انتهاء البناء تحصل على رابط لتحميل الـ `.ipa`.

### الرفع إلى App Store Connect
```bash
eas submit --platform ios --latest
```
أو رفع الملف يدويًا عبر تطبيق **Transporter** من Apple.

- الـ build يصل إلى **TestFlight** تلقائيًا.
- النشر للجمهور يتم من داخل **App Store Connect** بعد مراجعة Apple.

### البيتا (TestFlight)
- بعد أول رفع ناجح، التطبيق يظهر في **TestFlight** تلقائيًا.
- **اختبار داخلي**: حتى 100 مستخدم (بدون مراجعة Apple).
- **اختبار خارجي**: يحتاج موافقة "Beta App Review" من Apple، ثم يمكن دعوة مستخدمين عبر الإيميل.

---

## 📦 إذا أرسلت المشروع لشركة ليرفعوه عنك

اعطهم التالي:

1. **المشروع كامل** (مجلد TabibiQ-Mobile3) أو رابط الريبو إذا كان خاصًا.
2. **تعليمات واضحة**:
   - تشغيل: `npm install` ثم `eas build --platform ios --profile production`.
   - أو أن ترفع أنت الـ build وتُعطيهم ملف الـ `.ipa` ليرفعوه عبر Transporter.
3. **حساب Apple Developer**:
   - إما أن تضيفهم كـ **Team** في [App Store Connect](https://appstoreconnect.apple.com) (أدوار مناسبة مثل Admin أو Developer).
   - أو أن تعطيهم **Apple ID** و**كلمة المرور** (غير مستحسن أمنيًا – الأفضل إضافة فريق).
4. **تأكيد وجود** `GoogleService-Info.plist` في الجذر (لا يحذف أو يغيّر اسمه).
5. **رابط سياسة الخصوصية** النهائي إذا كان عندك.

---

## ⚠️ تنبيهات مهمة لـ Apple

1. **رقم البناء (Build Number)**  
   في كل رفع جديد يجب أن يزيد `ios.buildNumber` في `app.config.ts`.  
   حاليًا: `1.0.2`. الرفع القادم مثلاً: `1.0.3` أو `2` (يفضّل أن يكون فريدًا ومتزايدًا).

2. **رابط سياسة الخصوصية**  
   Apple يرفض التطبيق إذا كان حقل "Privacy Policy URL" فارغًا أو رابطًا لا يعمل. يجب نشر محتوى `PRIVACY_POLICY.md` على رابط https عام.

3. **تسجيل الدخول / المحتوى**  
   إذا كان المراجع يحتاج حسابًا لتجربة التطبيق، زوّده بـ **اختبار حساب** (مثلاً مستخدم وطبيب) في ملاحظات المراجعة.

4. **حذف الحساب (متطلبات المتجر)**  
   - التطبيق يوفر "حذف حسابي" من إعدادات الخصوصية.  
   - الطلب يرسل إلى الخادم `DELETE /user/:id` مع التوكن.  
   - الخادم يحذف فعلياً: المستخدم + المواعيد + التقييمات + الإشعارات + توكنات الإشعارات.  
   - لا يوجد "حذف وهمي" أو "تعطيل فقط" للمستخدم العادي؛ الحذف نهائي من قاعدة البيانات.

---

## الخلاصة

- **من ناحية الكود والإعدادات**: التطبيق جاهز للبناء والرفع على App Store.
- **ما يبقى**: حساب Apple Developer، إكمال بيانات App Store Connect، ووجود رابط لسياسة الخصوصية.
- **البيتا (TestFlight)** تصبح متاحة تلقائيًا بعد أول رفع ناجح للـ build.
